package main

func traverseWithChannel() {
	// init: make(chan *treeNode)

	// traverseFunc: send nodes into channel

	// iterate with channel: read nodes from channel
}

func main() {
	traverseWithChannel()
}
